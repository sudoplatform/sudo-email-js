/**
 * The Sudo Platform SDK representation of an enumaration depicting the sort order.
 *
 * @enum SortOrder
 */
export enum SortOrder {
  // Sort the list of results in ascending order.
  Asc = 'ASC',
  // Sort the list of results in descending order.
  Desc = 'DESC',
}
